const initialState = {
    username: '',
    id: 0,
    profilePicture: ''
}

const reducer = (state = initialState, action) => {
    switch(action.type){
        default:
            return state;
    }
}

export default reducer;